//! Event Module
//!
//! Generated by metaphor-schema. Enhanced with runtime implementations.
//!
//! This module provides:
//! - Domain entities and repositories
//! - Application services
//! - HTTP and gRPC handlers
//! - Route configuration
//! - State machine enforcement
//! - Validation rules runtime
//! - RBAC middleware
//! - Trigger execution system
//! - Computed fields
//! - Workflow orchestrator

#![recursion_limit = "1024"]
#![allow(unused_imports)]

// Generated modules
pub mod domain;
pub mod infrastructure;
pub mod application;
pub mod presentation;
pub mod seeders;
pub mod exports;

// Re-exports for convenience - Domain entities
pub use domain::entity::*;

// Re-exports - State Machine
pub use domain::state_machine::*;

// Re-exports - Infrastructure
pub use infrastructure::persistence::*;

// Re-exports - Application services
pub use application::service::EventService;
pub use application::service::EventAuditLogService;
pub use application::service::EventTypeService;
pub use application::service::MailService;
pub use application::service::MailRegistrationService;
pub use application::service::MailSlotService;
pub use application::service::QuestionService;
pub use application::service::QuestionAnswerService;
pub use application::service::RegistrationQuestionService;
pub use application::service::RegistrationService;
pub use application::service::RegistrationAnswerService;
pub use application::service::SlotService;
pub use application::service::StageService;
pub use application::service::TagCategoryService;
pub use application::service::TagService;
pub use application::service::TicketService;
pub use application::service::TypeMailService;

use std::sync::Arc;
use axum::Router;
use sqlx::PgPool;

/// Event module configuration
///
/// Use the builder pattern to configure and register this module:
///
/// ```text
/// let event = EventModule::builder()
///     .with_database(pool.clone())
///     .build()?;
///
/// // Unguarded full CRUD (trusted/admin); compose a guarded router for production.
/// let router = event.all_crud_routes();
/// ```
pub struct EventModule {
    pub(crate) event_service: Arc<EventService>,
    pub(crate) event_audit_log_service: Arc<EventAuditLogService>,
    pub(crate) event_type_service: Arc<EventTypeService>,
    pub(crate) mail_service: Arc<MailService>,
    pub(crate) mail_registration_service: Arc<MailRegistrationService>,
    pub(crate) mail_slot_service: Arc<MailSlotService>,
    pub(crate) question_service: Arc<QuestionService>,
    pub(crate) question_answer_service: Arc<QuestionAnswerService>,
    pub(crate) registration_question_service: Arc<RegistrationQuestionService>,
    pub(crate) registration_service: Arc<RegistrationService>,
    pub(crate) registration_answer_service: Arc<RegistrationAnswerService>,
    pub(crate) slot_service: Arc<SlotService>,
    pub(crate) stage_service: Arc<StageService>,
    pub(crate) tag_category_service: Arc<TagCategoryService>,
    pub(crate) tag_service: Arc<TagService>,
    pub(crate) ticket_service: Arc<TicketService>,
    pub(crate) type_mail_service: Arc<TypeMailService>,
    // <<< CUSTOM FIELDS
    // END CUSTOM
}

impl EventModule {
    /// Create a new module builder
    pub fn builder() -> EventModuleBuilder {
        EventModuleBuilder::new()
    }

    /// Mount ALL generated CRUD endpoints (12 per entity) with NO domain
    /// validation — the fully **unguarded** surface. A well-formed request can
    /// create invalid rows or soft-delete a referenced master out from under its
    /// dependents. Prefer a guarded composition (read + validated writes) for any
    /// real deployment; use this only in trusted/admin/seeding contexts.
    pub fn all_crud_routes(&self) -> Router {
        use presentation::http::{
            create_event_read_routes,
            create_event_audit_log_read_routes,
            create_event_type_routes,
            create_mail_read_routes,
            create_mail_registration_routes,
            create_mail_slot_routes,
            create_question_routes,
            create_question_answer_routes,
            create_registration_question_routes,
            create_registration_read_routes,
            create_registration_answer_routes,
            create_slot_routes,
            create_stage_routes,
            create_tag_category_routes,
            create_tag_routes,
            create_ticket_routes,
            create_type_mail_routes,
        };

        Router::new()
            .merge(create_event_read_routes(self.event_service.clone()))
            .merge(create_event_audit_log_read_routes(self.event_audit_log_service.clone()))
            .merge(create_event_type_routes(self.event_type_service.clone()))
            .merge(create_mail_read_routes(self.mail_service.clone()))
            .merge(create_mail_registration_routes(self.mail_registration_service.clone()))
            .merge(create_mail_slot_routes(self.mail_slot_service.clone()))
            .merge(create_question_routes(self.question_service.clone()))
            .merge(create_question_answer_routes(self.question_answer_service.clone()))
            .merge(create_registration_question_routes(self.registration_question_service.clone()))
            .merge(create_registration_read_routes(self.registration_service.clone()))
            .merge(create_registration_answer_routes(self.registration_answer_service.clone()))
            .merge(create_slot_routes(self.slot_service.clone()))
            .merge(create_stage_routes(self.stage_service.clone()))
            .merge(create_tag_category_routes(self.tag_category_service.clone()))
            .merge(create_tag_routes(self.tag_service.clone()))
            .merge(create_ticket_routes(self.ticket_service.clone()))
            .merge(create_type_mail_routes(self.type_mail_service.clone()))
    }

    /// Deprecated alias for [`Self::all_crud_routes`]. `routes()` reads like
    /// "the routes" but mounts UNVALIDATED generic CRUD on every entity — a naive
    /// mount exposes unguarded writes. Compose a guarded router (read + validated
    /// writes) for production, or call `all_crud_routes()` to opt into the full
    /// unguarded surface explicitly.
    #[deprecated(note = "mounts unvalidated generic CRUD; prefer readonly_routes() + validated writes, or all_crud_routes() for the full/unguarded surface")]
    pub fn routes(&self) -> Router {
        self.all_crud_routes()
    }

    /// Read-only routes for every entity (GET endpoints only) — the safe base.
    ///
    /// Generic mutation can't reach here, so this surface cannot bypass a
    /// validated write service's invariants. Use this as the production base and
    /// merge validated write routes (or a write service's HTTP layer) onto it.
    pub fn readonly_routes(&self) -> Router {
        use presentation::http::{
            create_event_read_routes,
            create_event_audit_log_read_routes,
            create_event_type_read_routes,
            create_mail_read_routes,
            create_mail_registration_read_routes,
            create_mail_slot_read_routes,
            create_question_read_routes,
            create_question_answer_read_routes,
            create_registration_question_read_routes,
            create_registration_read_routes,
            create_registration_answer_read_routes,
            create_slot_read_routes,
            create_stage_read_routes,
            create_tag_category_read_routes,
            create_tag_read_routes,
            create_ticket_read_routes,
            create_type_mail_read_routes,
        };

        Router::new()
            .merge(create_event_read_routes(self.event_service.clone()))
            .merge(create_event_audit_log_read_routes(self.event_audit_log_service.clone()))
            .merge(create_event_type_read_routes(self.event_type_service.clone()))
            .merge(create_mail_read_routes(self.mail_service.clone()))
            .merge(create_mail_registration_read_routes(self.mail_registration_service.clone()))
            .merge(create_mail_slot_read_routes(self.mail_slot_service.clone()))
            .merge(create_question_read_routes(self.question_service.clone()))
            .merge(create_question_answer_read_routes(self.question_answer_service.clone()))
            .merge(create_registration_question_read_routes(self.registration_question_service.clone()))
            .merge(create_registration_read_routes(self.registration_service.clone()))
            .merge(create_registration_answer_read_routes(self.registration_answer_service.clone()))
            .merge(create_slot_read_routes(self.slot_service.clone()))
            .merge(create_stage_read_routes(self.stage_service.clone()))
            .merge(create_tag_category_read_routes(self.tag_category_service.clone()))
            .merge(create_tag_read_routes(self.tag_service.clone()))
            .merge(create_ticket_read_routes(self.ticket_service.clone()))
            .merge(create_type_mail_read_routes(self.type_mail_service.clone()))
    }

    // <<< CUSTOM METHODS
    // END CUSTOM
}

/// Builder for EventModule
pub struct EventModuleBuilder {
    db_pool: Option<PgPool>,
}

impl EventModuleBuilder {
    /// Create a new builder
    pub fn new() -> Self {
        Self {
            db_pool: None,
        }
    }

    /// Set the database connection pool
    pub fn with_database(mut self, pool: PgPool) -> Self {
        self.db_pool = Some(pool);
        self
    }

    // <<< CUSTOM - custom builder methods
    // END CUSTOM

    /// Build the module with configured dependencies
    pub fn build(self) -> anyhow::Result<EventModule> {
        let db_pool = self.db_pool
            .ok_or_else(|| anyhow::anyhow!("Database pool not configured"))?;

        // Event service
        let event_repository = Arc::new(EventRepository::new(db_pool.clone()));
        let event_service = Arc::new(EventService::with_repository(event_repository.clone()));

        // EventAuditLog service
        let event_audit_log_repository = Arc::new(EventAuditLogRepository::new(db_pool.clone()));
        let event_audit_log_service = Arc::new(EventAuditLogService::with_repository(event_audit_log_repository.clone()));

        // EventType service
        let event_type_repository = Arc::new(EventTypeRepository::new(db_pool.clone()));
        let event_type_service = Arc::new(EventTypeService::with_repository(event_type_repository.clone()));

        // Mail service
        let mail_repository = Arc::new(MailRepository::new(db_pool.clone()));
        let mail_service = Arc::new(MailService::with_repository(mail_repository.clone()));

        // MailRegistration service
        let mail_registration_repository = Arc::new(MailRegistrationRepository::new(db_pool.clone()));
        let mail_registration_service = Arc::new(MailRegistrationService::with_repository(mail_registration_repository.clone()));

        // MailSlot service
        let mail_slot_repository = Arc::new(MailSlotRepository::new(db_pool.clone()));
        let mail_slot_service = Arc::new(MailSlotService::with_repository(mail_slot_repository.clone()));

        // Question service
        let question_repository = Arc::new(QuestionRepository::new(db_pool.clone()));
        let question_service = Arc::new(QuestionService::with_repository(question_repository.clone()));

        // QuestionAnswer service
        let question_answer_repository = Arc::new(QuestionAnswerRepository::new(db_pool.clone()));
        let question_answer_service = Arc::new(QuestionAnswerService::with_repository(question_answer_repository.clone()));

        // RegistrationQuestion service
        let registration_question_repository = Arc::new(RegistrationQuestionRepository::new(db_pool.clone()));
        let registration_question_service = Arc::new(RegistrationQuestionService::with_repository(registration_question_repository.clone()));

        // Registration service
        let registration_repository = Arc::new(RegistrationRepository::new(db_pool.clone()));
        let registration_service = Arc::new(RegistrationService::with_repository(registration_repository.clone()));

        // RegistrationAnswer service
        let registration_answer_repository = Arc::new(RegistrationAnswerRepository::new(db_pool.clone()));
        let registration_answer_service = Arc::new(RegistrationAnswerService::with_repository(registration_answer_repository.clone()));

        // Slot service
        let slot_repository = Arc::new(SlotRepository::new(db_pool.clone()));
        let slot_service = Arc::new(SlotService::with_repository(slot_repository.clone()));

        // Stage service
        let stage_repository = Arc::new(StageRepository::new(db_pool.clone()));
        let stage_service = Arc::new(StageService::with_repository(stage_repository.clone()));

        // TagCategory service
        let tag_category_repository = Arc::new(TagCategoryRepository::new(db_pool.clone()));
        let tag_category_service = Arc::new(TagCategoryService::with_repository(tag_category_repository.clone()));

        // Tag service
        let tag_repository = Arc::new(TagRepository::new(db_pool.clone()));
        let tag_service = Arc::new(TagService::with_repository(tag_repository.clone()));

        // Ticket service
        let ticket_repository = Arc::new(TicketRepository::new(db_pool.clone()));
        let ticket_service = Arc::new(TicketService::with_repository(ticket_repository.clone()));

        // TypeMail service
        let type_mail_repository = Arc::new(TypeMailRepository::new(db_pool.clone()));
        let type_mail_service = Arc::new(TypeMailService::with_repository(type_mail_repository.clone()));

        // <<< CUSTOM
        // END CUSTOM

        Ok(EventModule {
            event_service,
            event_audit_log_service,
            event_type_service,
            mail_service,
            mail_registration_service,
            mail_slot_service,
            question_service,
            question_answer_service,
            registration_question_service,
            registration_service,
            registration_answer_service,
            slot_service,
            stage_service,
            tag_category_service,
            tag_service,
            ticket_service,
            type_mail_service,
            // <<< CUSTOM
            // END CUSTOM
        })
    }
}

impl Default for EventModuleBuilder {
    fn default() -> Self {
        Self::new()
    }
}
