//! LeadRequest REST handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Uses Axum and backbone-core's BackboneCrudHandler for all 12 CRUD endpoints.

use std::collections::HashMap;
use std::sync::Arc;

use axum::Router;
use serde::{Deserialize, Serialize};
use uuid::Uuid;
use chrono::{DateTime, Utc};

// Backbone framework imports
use backbone_core::http::BackboneCrudHandler;

// Auth integration (optional)
#[cfg(feature = "auth")]
use backbone_auth::middleware::AuthContext;
#[cfg(feature = "auth")]
use backbone_auth::AuthMiddleware;

// Domain imports
use crate::domain::entity::*;
use crate::application::service::{LeadRequestService, ServiceError};

// DTO imports
use crate::presentation::dto::{CreateLeadRequestDto, UpdateLeadRequestDto, PatchLeadRequestDto, LeadRequestResponseDto};


/// Application error type
#[derive(Debug, thiserror::Error)]
pub enum LeadRequestError {
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Validation error: {0}")]
    Validation(String),
    #[error("Database error: {0}")]
    Database(String),
    #[error("Internal error: {0}")]
    Internal(String),
}

impl From<ServiceError> for LeadRequestError {
    fn from(err: ServiceError) -> Self {
        match err {
            ServiceError::NotFound => Self::NotFound(err.to_string()),
            ServiceError::Validation(ref msg) => Self::Validation(msg.clone()),
            ServiceError::AlreadyExists(ref msg) => Self::Validation(msg.clone()),
            ServiceError::Repository(ref e) => Self::Database(e.to_string()),
            ServiceError::Internal(ref msg) => Self::Internal(msg.clone()),
        }
    }
}

impl axum::response::IntoResponse for LeadRequestError {
    fn into_response(self) -> axum::response::Response {
        use axum::http::StatusCode;
        use axum::Json;

        let (status, code) = match &self {
            Self::NotFound(_) => (StatusCode::NOT_FOUND, "LEADREQUEST_NOT_FOUND"),
            Self::Validation(_) => (StatusCode::BAD_REQUEST, "LEADREQUEST_VALIDATION_ERROR"),
            Self::Database(_) => (StatusCode::INTERNAL_SERVER_ERROR, "LEADREQUEST_DATABASE_ERROR"),
            Self::Internal(_) => (StatusCode::INTERNAL_SERVER_ERROR, "LEADREQUEST_INTERNAL_ERROR"),
        };

        let body = serde_json::json!({
            "success": false,
            "error": code,
            "message": self.to_string(),
        });

        (status, Json(body)).into_response()
    }
}

// =============================================================================
// Route Configuration
// =============================================================================

/// Create Axum router with all 16 Backbone endpoints for LeadRequest.
///
/// # Routes
///
/// | Method | Path | Description |
/// |--------|------|-------------|
/// | GET | /lead_requests | List with pagination |
/// | POST | /lead_requests | Create new |
/// | GET | /lead_requests/:id | Get by ID |
/// | PUT | /lead_requests/:id | Full update |
/// | PATCH | /lead_requests/:id | Partial update |
/// | DELETE | /lead_requests/:id | Soft delete |
/// | POST | /lead_requests/bulk | Bulk create |
/// | POST | /lead_requests/upsert | Upsert |
/// | GET | /lead_requests/trash | List deleted |
/// | POST | /lead_requests/:id/restore | Restore |
/// | DELETE | /lead_requests/empty | Empty trash |
/// | GET | /lead_requests/:id/deleted | Get deleted by ID |
/// | DELETE | /lead_requests/trash/:id | Permanent delete from trash |
/// | GET | /lead_requests/count | Count active entities |
/// | GET | /lead_requests/trash/count | Count deleted entities |
///
/// # Example
///
/// ```text
/// let service = Arc::new(LeadRequestService::with_repository(repository));
/// let router = create_lead_request_routes(service);
/// ```
pub fn create_lead_request_routes(service: Arc<LeadRequestService>) -> Router {
    BackboneCrudHandler::<LeadRequestService, LeadRequest, CreateLeadRequestDto, UpdateLeadRequestDto, LeadRequestResponseDto>::routes(
        service,
        "/lead_requests",
    )
}

/// Create Axum router with only the read (GET) endpoints for LeadRequest.
///
/// Safe for public, unauthenticated exposure (e.g., reference data).
/// Mutations must be served separately via `create_lead_request_write_routes`,
/// typically wrapped in an auth middleware layer.
pub fn create_lead_request_read_routes(service: Arc<LeadRequestService>) -> Router {
    BackboneCrudHandler::<LeadRequestService, LeadRequest, CreateLeadRequestDto, UpdateLeadRequestDto, LeadRequestResponseDto>::read_routes(
        service,
        "/lead_requests",
    )
}

/// Create Axum router with only the write (mutation) endpoints for LeadRequest.
///
/// These routes must NOT be publicly exposed. Wrap them with an auth
/// middleware before nesting into the application router.
///
/// # This is unguarded generic CRUD, not a validated write path
///
/// These are plain create/update/patch/delete mutations over the entity row —
/// they bypass all business invariants. If the module exposes a validated write
/// service (e.g. a command router over its domain engine), serve THAT instead
/// for any mutation that must respect domain rules.
pub fn create_lead_request_write_routes(service: Arc<LeadRequestService>) -> Router {
    BackboneCrudHandler::<LeadRequestService, LeadRequest, CreateLeadRequestDto, UpdateLeadRequestDto, LeadRequestResponseDto>::write_routes(
        service,
        "/lead_requests",
    )
}

/// Create authenticated routes with auth middleware.
///
/// Requires the `auth` feature flag. The `AuthMiddleware` implementation
/// is responsible for extracting and validating tokens, then providing
/// an `AuthContext` via request extensions.
#[cfg(feature = "auth")]
pub fn create_protected_lead_request_routes<A: AuthMiddleware + Send + Sync + 'static>(
    service: Arc<LeadRequestService>,
    auth: Arc<A>,
) -> Router {
    use axum::middleware;
    use axum::response::IntoResponse;

    let auth_layer = auth.clone();
    create_lead_request_routes(service)
        .layer(middleware::from_fn(move |mut req: axum::extract::Request, next: axum::middleware::Next| {
            let auth = auth_layer.clone();
            async move {
                let token = req.headers()
                    .get(axum::http::header::AUTHORIZATION)
                    .and_then(|h| h.to_str().ok())
                    .and_then(|raw| raw.strip_prefix("Bearer ").or_else(|| raw.strip_prefix("bearer ")))
                    .unwrap_or("");
                match auth.authenticate(token).await {
                    Ok(ctx) => {
                        req.extensions_mut().insert(ctx);
                        next.run(req).await
                    }
                    Err(_) => {
                        (axum::http::StatusCode::UNAUTHORIZED,
                         axum::Json(serde_json::json!({
                             "success": false,
                             "error": "unauthorized",
                             "message": "Authentication required"
                         }))
                        ).into_response()
                    }
                }
            }
        }))
}
