//! Domain events for RegistrationQuestion entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::RegistrationQuestion;

/// Domain events for RegistrationQuestion — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<RegistrationQuestion>)` variant.
pub type RegistrationQuestionEvent = CrudEvent<RegistrationQuestion>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for RegistrationQuestion events.
pub type RegistrationQuestionEventPublisher = Arc<dyn CrudEventPublisher<RegistrationQuestion> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopRegistrationQuestionEventPublisher = NoOpCrudEventPublisher<RegistrationQuestion>;
