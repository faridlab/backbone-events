//! Domain events for TypeMail entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::TypeMail;

/// Domain events for TypeMail — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<TypeMail>)` variant.
pub type TypeMailEvent = CrudEvent<TypeMail>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for TypeMail events.
pub type TypeMailEventPublisher = Arc<dyn CrudEventPublisher<TypeMail> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopTypeMailEventPublisher = NoOpCrudEventPublisher<TypeMail>;
