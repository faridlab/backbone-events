//! Domain events for MailRegistration entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::MailRegistration;

/// Domain events for MailRegistration — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<MailRegistration>)` variant.
pub type MailRegistrationEvent = CrudEvent<MailRegistration>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for MailRegistration events.
pub type MailRegistrationEventPublisher = Arc<dyn CrudEventPublisher<MailRegistration> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopMailRegistrationEventPublisher = NoOpCrudEventPublisher<MailRegistration>;
