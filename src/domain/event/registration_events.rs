//! Domain events for Registration entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Registration;

/// Domain events for Registration — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Registration>)` variant.
pub type RegistrationEvent = CrudEvent<Registration>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Registration events.
pub type RegistrationEventPublisher = Arc<dyn CrudEventPublisher<Registration> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopRegistrationEventPublisher = NoOpCrudEventPublisher<Registration>;
