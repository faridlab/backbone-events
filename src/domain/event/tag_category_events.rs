//! Domain events for TagCategory entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::TagCategory;

/// Domain events for TagCategory — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<TagCategory>)` variant.
pub type TagCategoryEvent = CrudEvent<TagCategory>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for TagCategory events.
pub type TagCategoryEventPublisher = Arc<dyn CrudEventPublisher<TagCategory> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopTagCategoryEventPublisher = NoOpCrudEventPublisher<TagCategory>;
