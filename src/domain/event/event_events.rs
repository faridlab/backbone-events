//! Domain events for Event entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Event;

/// Domain events for Event — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Event>)` variant.
pub type EventEvent = CrudEvent<Event>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Event events.
pub type EventEventPublisher = Arc<dyn CrudEventPublisher<Event> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopEventEventPublisher = NoOpCrudEventPublisher<Event>;
