//! Domain events for Tag entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Tag;

/// Domain events for Tag — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Tag>)` variant.
pub type TagEvent = CrudEvent<Tag>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Tag events.
pub type TagEventPublisher = Arc<dyn CrudEventPublisher<Tag> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopTagEventPublisher = NoOpCrudEventPublisher<Tag>;
