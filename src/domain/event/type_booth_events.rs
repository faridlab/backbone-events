//! Domain events for TypeBooth entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::TypeBooth;

/// Domain events for TypeBooth — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<TypeBooth>)` variant.
pub type TypeBoothEvent = CrudEvent<TypeBooth>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for TypeBooth events.
pub type TypeBoothEventPublisher = Arc<dyn CrudEventPublisher<TypeBooth> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopTypeBoothEventPublisher = NoOpCrudEventPublisher<TypeBooth>;
