//! Domain events for LeadRule entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::LeadRule;

/// Domain events for LeadRule — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<LeadRule>)` variant.
pub type LeadRuleEvent = CrudEvent<LeadRule>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for LeadRule events.
pub type LeadRuleEventPublisher = Arc<dyn CrudEventPublisher<LeadRule> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopLeadRuleEventPublisher = NoOpCrudEventPublisher<LeadRule>;
