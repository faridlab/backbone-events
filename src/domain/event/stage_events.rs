//! Domain events for Stage entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Stage;

/// Domain events for Stage — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Stage>)` variant.
pub type StageEvent = CrudEvent<Stage>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Stage events.
pub type StageEventPublisher = Arc<dyn CrudEventPublisher<Stage> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopStageEventPublisher = NoOpCrudEventPublisher<Stage>;
