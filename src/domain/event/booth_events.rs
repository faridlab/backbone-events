//! Domain events for Booth entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Booth;

/// Domain events for Booth — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Booth>)` variant.
pub type BoothEvent = CrudEvent<Booth>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Booth events.
pub type BoothEventPublisher = Arc<dyn CrudEventPublisher<Booth> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopBoothEventPublisher = NoOpCrudEventPublisher<Booth>;
