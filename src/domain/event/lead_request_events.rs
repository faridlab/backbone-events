//! Domain events for LeadRequest entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::LeadRequest;

/// Domain events for LeadRequest — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<LeadRequest>)` variant.
pub type LeadRequestEvent = CrudEvent<LeadRequest>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for LeadRequest events.
pub type LeadRequestEventPublisher = Arc<dyn CrudEventPublisher<LeadRequest> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopLeadRequestEventPublisher = NoOpCrudEventPublisher<LeadRequest>;
