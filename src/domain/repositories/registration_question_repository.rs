//! Repository trait for RegistrationQuestion aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the RegistrationQuestion aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::RegistrationQuestion;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct RegistrationQuestionPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl RegistrationQuestionPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct RegistrationQuestionPaginatedResult {
    pub data: Vec<RegistrationQuestion>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct RegistrationQuestionFilter {
    pub event_id: Option<Uuid>,
    pub event_type_id: Option<Uuid>,
    pub question_id: Option<Uuid>,
}

impl RegistrationQuestionFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.event_id.is_some() || self.event_type_id.is_some() || self.question_id.is_some()
    }
}

/// Repository trait for RegistrationQuestion aggregate
///
/// This trait defines the persistence contract for RegistrationQuestion entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait RegistrationQuestionRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new registration_question entity
    async fn save(&self, entity: &RegistrationQuestion) -> Result<RegistrationQuestion>;

    /// Find registration_question by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<RegistrationQuestion>>;

    /// Find all registration_question entities
    async fn find_all(&self) -> Result<Vec<RegistrationQuestion>>;

    /// Update registration_question by ID
    async fn update(&self, id: &str, entity: &RegistrationQuestion) -> Result<Option<RegistrationQuestion>>;

    /// Delete registration_question by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List registration_question with pagination
    async fn list(&self, params: RegistrationQuestionPaginationParams) -> Result<RegistrationQuestionPaginatedResult>;

    /// List registration_question with pagination and filters
    async fn list_with_filters(&self, params: RegistrationQuestionPaginationParams, filters: RegistrationQuestionFilter) -> Result<RegistrationQuestionPaginatedResult>;

    /// Count all registration_question entities
    async fn count(&self) -> Result<u64>;

    /// Count registration_question entities matching filters
    async fn count_with_filters(&self, filters: RegistrationQuestionFilter) -> Result<u64>;

    /// Check if registration_question exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete registration_question by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted registration_question by ID
    async fn restore(&self, id: &str) -> Result<Option<RegistrationQuestion>>;

    /// List soft-deleted registration_question entities
    async fn list_deleted(&self, params: RegistrationQuestionPaginationParams) -> Result<RegistrationQuestionPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save registration_question entities
    async fn bulk_save(&self, entities: &[RegistrationQuestion]) -> Result<Vec<RegistrationQuestion>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
