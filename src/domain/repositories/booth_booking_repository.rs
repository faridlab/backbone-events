//! Repository trait for BoothBooking aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the BoothBooking aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{BoothBooking, EventBoothBookingStatus};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct BoothBookingPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl BoothBookingPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct BoothBookingPaginatedResult {
    pub data: Vec<BoothBooking>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct BoothBookingFilter {
    pub event_booth_id: Option<Uuid>,
    pub sale_order_line_id: Option<Uuid>,
    pub status: Option<EventBoothBookingStatus>,
    pub partner_id: Option<Uuid>,
    pub contact_name: Option<String>,
    pub contact_email: Option<String>,
    pub contact_phone: Option<String>,
}

impl BoothBookingFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.event_booth_id.is_some() || self.sale_order_line_id.is_some() || self.status.is_some() || self.partner_id.is_some() || self.contact_name.is_some() || self.contact_email.is_some() || self.contact_phone.is_some()
    }
}

/// Repository trait for BoothBooking aggregate
///
/// This trait defines the persistence contract for BoothBooking entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait BoothBookingRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new booth_booking entity
    async fn save(&self, entity: &BoothBooking) -> Result<BoothBooking>;

    /// Find booth_booking by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<BoothBooking>>;

    /// Find all booth_booking entities
    async fn find_all(&self) -> Result<Vec<BoothBooking>>;

    /// Update booth_booking by ID
    async fn update(&self, id: &str, entity: &BoothBooking) -> Result<Option<BoothBooking>>;

    /// Delete booth_booking by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List booth_booking with pagination
    async fn list(&self, params: BoothBookingPaginationParams) -> Result<BoothBookingPaginatedResult>;

    /// List booth_booking with pagination and filters
    async fn list_with_filters(&self, params: BoothBookingPaginationParams, filters: BoothBookingFilter) -> Result<BoothBookingPaginatedResult>;

    /// Count all booth_booking entities
    async fn count(&self) -> Result<u64>;

    /// Count booth_booking entities matching filters
    async fn count_with_filters(&self, filters: BoothBookingFilter) -> Result<u64>;

    /// Check if booth_booking exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete booth_booking by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted booth_booking by ID
    async fn restore(&self, id: &str) -> Result<Option<BoothBooking>>;

    /// List soft-deleted booth_booking entities
    async fn list_deleted(&self, params: BoothBookingPaginationParams) -> Result<BoothBookingPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save booth_booking entities
    async fn bulk_save(&self, entities: &[BoothBooking]) -> Result<Vec<BoothBooking>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
