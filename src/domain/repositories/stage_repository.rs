//! Repository trait for Stage aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the Stage aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;

use crate::domain::entity::Stage;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct StagePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl StagePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct StagePaginatedResult {
    pub data: Vec<Stage>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct StageFilter {
    pub name: Option<String>,
    pub fold: Option<bool>,
    pub pipe_end: Option<bool>,
}

impl StageFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.fold.is_some() || self.pipe_end.is_some()
    }
}

/// Repository trait for Stage aggregate
///
/// This trait defines the persistence contract for Stage entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait StageRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new stage entity
    async fn save(&self, entity: &Stage) -> Result<Stage>;

    /// Find stage by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<Stage>>;

    /// Find all stage entities
    async fn find_all(&self) -> Result<Vec<Stage>>;

    /// Update stage by ID
    async fn update(&self, id: &str, entity: &Stage) -> Result<Option<Stage>>;

    /// Delete stage by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List stage with pagination
    async fn list(&self, params: StagePaginationParams) -> Result<StagePaginatedResult>;

    /// List stage with pagination and filters
    async fn list_with_filters(&self, params: StagePaginationParams, filters: StageFilter) -> Result<StagePaginatedResult>;

    /// Count all stage entities
    async fn count(&self) -> Result<u64>;

    /// Count stage entities matching filters
    async fn count_with_filters(&self, filters: StageFilter) -> Result<u64>;

    /// Check if stage exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete stage by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted stage by ID
    async fn restore(&self, id: &str) -> Result<Option<Stage>>;

    /// List soft-deleted stage entities
    async fn list_deleted(&self, params: StagePaginationParams) -> Result<StagePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save stage entities
    async fn bulk_save(&self, entities: &[Stage]) -> Result<Vec<Stage>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
