//! Repository trait for LeadRule aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the LeadRule aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{LeadRule, EventLeadBasis};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct LeadRulePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl LeadRulePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct LeadRulePaginatedResult {
    pub data: Vec<LeadRule>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct LeadRuleFilter {
    pub name: Option<String>,
    pub event_id: Option<Uuid>,
    pub basis: Option<EventLeadBasis>,
    pub on_create: Option<bool>,
    pub on_confirm: Option<bool>,
    pub on_done: Option<bool>,
    pub active: Option<bool>,
}

impl LeadRuleFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.event_id.is_some() || self.basis.is_some() || self.on_create.is_some() || self.on_confirm.is_some() || self.on_done.is_some() || self.active.is_some()
    }
}

/// Repository trait for LeadRule aggregate
///
/// This trait defines the persistence contract for LeadRule entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait LeadRuleRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new lead_rule entity
    async fn save(&self, entity: &LeadRule) -> Result<LeadRule>;

    /// Find lead_rule by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<LeadRule>>;

    /// Find all lead_rule entities
    async fn find_all(&self) -> Result<Vec<LeadRule>>;

    /// Update lead_rule by ID
    async fn update(&self, id: &str, entity: &LeadRule) -> Result<Option<LeadRule>>;

    /// Delete lead_rule by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List lead_rule with pagination
    async fn list(&self, params: LeadRulePaginationParams) -> Result<LeadRulePaginatedResult>;

    /// List lead_rule with pagination and filters
    async fn list_with_filters(&self, params: LeadRulePaginationParams, filters: LeadRuleFilter) -> Result<LeadRulePaginatedResult>;

    /// Count all lead_rule entities
    async fn count(&self) -> Result<u64>;

    /// Count lead_rule entities matching filters
    async fn count_with_filters(&self, filters: LeadRuleFilter) -> Result<u64>;

    /// Check if lead_rule exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete lead_rule by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted lead_rule by ID
    async fn restore(&self, id: &str) -> Result<Option<LeadRule>>;

    /// List soft-deleted lead_rule entities
    async fn list_deleted(&self, params: LeadRulePaginationParams) -> Result<LeadRulePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save lead_rule entities
    async fn bulk_save(&self, entities: &[LeadRule]) -> Result<Vec<LeadRule>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
