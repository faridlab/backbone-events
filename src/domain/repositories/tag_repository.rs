//! Repository trait for Tag aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the Tag aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::Tag;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct TagPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl TagPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct TagPaginatedResult {
    pub data: Vec<Tag>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct TagFilter {
    pub name: Option<String>,
    pub category_id: Option<Uuid>,
    pub color: Option<String>,
}

impl TagFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.category_id.is_some() || self.color.is_some()
    }
}

/// Repository trait for Tag aggregate
///
/// This trait defines the persistence contract for Tag entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait TagRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new tag entity
    async fn save(&self, entity: &Tag) -> Result<Tag>;

    /// Find tag by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<Tag>>;

    /// Find all tag entities
    async fn find_all(&self) -> Result<Vec<Tag>>;

    /// Update tag by ID
    async fn update(&self, id: &str, entity: &Tag) -> Result<Option<Tag>>;

    /// Delete tag by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List tag with pagination
    async fn list(&self, params: TagPaginationParams) -> Result<TagPaginatedResult>;

    /// List tag with pagination and filters
    async fn list_with_filters(&self, params: TagPaginationParams, filters: TagFilter) -> Result<TagPaginatedResult>;

    /// Count all tag entities
    async fn count(&self) -> Result<u64>;

    /// Count tag entities matching filters
    async fn count_with_filters(&self, filters: TagFilter) -> Result<u64>;

    /// Check if tag exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete tag by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted tag by ID
    async fn restore(&self, id: &str) -> Result<Option<Tag>>;

    /// List soft-deleted tag entities
    async fn list_deleted(&self, params: TagPaginationParams) -> Result<TagPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save tag entities
    async fn bulk_save(&self, entities: &[Tag]) -> Result<Vec<Tag>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
