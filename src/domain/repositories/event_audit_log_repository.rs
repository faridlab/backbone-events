//! Repository trait for EventAuditLog aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the EventAuditLog aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{EventAuditLog, EventAuditEvent};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct EventAuditLogPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl EventAuditLogPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct EventAuditLogPaginatedResult {
    pub data: Vec<EventAuditLog>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct EventAuditLogFilter {
    pub event: Option<EventAuditEvent>,
    pub actor: Option<Uuid>,
    pub subject_type: Option<String>,
    pub subject_id: Option<Uuid>,
}

impl EventAuditLogFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.event.is_some() || self.actor.is_some() || self.subject_type.is_some() || self.subject_id.is_some()
    }
}

/// Repository trait for EventAuditLog aggregate
///
/// This trait defines the persistence contract for EventAuditLog entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait EventAuditLogRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new event_audit_log entity
    async fn save(&self, entity: &EventAuditLog) -> Result<EventAuditLog>;

    /// Find event_audit_log by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<EventAuditLog>>;

    /// Find all event_audit_log entities
    async fn find_all(&self) -> Result<Vec<EventAuditLog>>;

    /// Update event_audit_log by ID
    async fn update(&self, id: &str, entity: &EventAuditLog) -> Result<Option<EventAuditLog>>;

    /// Delete event_audit_log by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List event_audit_log with pagination
    async fn list(&self, params: EventAuditLogPaginationParams) -> Result<EventAuditLogPaginatedResult>;

    /// List event_audit_log with pagination and filters
    async fn list_with_filters(&self, params: EventAuditLogPaginationParams, filters: EventAuditLogFilter) -> Result<EventAuditLogPaginatedResult>;

    /// Count all event_audit_log entities
    async fn count(&self) -> Result<u64>;

    /// Count event_audit_log entities matching filters
    async fn count_with_filters(&self, filters: EventAuditLogFilter) -> Result<u64>;

    /// Check if event_audit_log exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save event_audit_log entities
    async fn bulk_save(&self, entities: &[EventAuditLog]) -> Result<Vec<EventAuditLog>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
