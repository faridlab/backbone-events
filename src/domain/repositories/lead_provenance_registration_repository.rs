//! Repository trait for LeadProvenanceRegistration aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the LeadProvenanceRegistration aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::LeadProvenanceRegistration;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct LeadProvenanceRegistrationPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl LeadProvenanceRegistrationPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct LeadProvenanceRegistrationPaginatedResult {
    pub data: Vec<LeadProvenanceRegistration>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct LeadProvenanceRegistrationFilter {
    pub provenance_id: Option<Uuid>,
    pub registration_id: Option<Uuid>,
}

impl LeadProvenanceRegistrationFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.provenance_id.is_some() || self.registration_id.is_some()
    }
}

/// Repository trait for LeadProvenanceRegistration aggregate
///
/// This trait defines the persistence contract for LeadProvenanceRegistration entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait LeadProvenanceRegistrationRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new lead_provenance_registration entity
    async fn save(&self, entity: &LeadProvenanceRegistration) -> Result<LeadProvenanceRegistration>;

    /// Find lead_provenance_registration by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<LeadProvenanceRegistration>>;

    /// Find all lead_provenance_registration entities
    async fn find_all(&self) -> Result<Vec<LeadProvenanceRegistration>>;

    /// Update lead_provenance_registration by ID
    async fn update(&self, id: &str, entity: &LeadProvenanceRegistration) -> Result<Option<LeadProvenanceRegistration>>;

    /// Delete lead_provenance_registration by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List lead_provenance_registration with pagination
    async fn list(&self, params: LeadProvenanceRegistrationPaginationParams) -> Result<LeadProvenanceRegistrationPaginatedResult>;

    /// List lead_provenance_registration with pagination and filters
    async fn list_with_filters(&self, params: LeadProvenanceRegistrationPaginationParams, filters: LeadProvenanceRegistrationFilter) -> Result<LeadProvenanceRegistrationPaginatedResult>;

    /// Count all lead_provenance_registration entities
    async fn count(&self) -> Result<u64>;

    /// Count lead_provenance_registration entities matching filters
    async fn count_with_filters(&self, filters: LeadProvenanceRegistrationFilter) -> Result<u64>;

    /// Check if lead_provenance_registration exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete lead_provenance_registration by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted lead_provenance_registration by ID
    async fn restore(&self, id: &str) -> Result<Option<LeadProvenanceRegistration>>;

    /// List soft-deleted lead_provenance_registration entities
    async fn list_deleted(&self, params: LeadProvenanceRegistrationPaginationParams) -> Result<LeadProvenanceRegistrationPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save lead_provenance_registration entities
    async fn bulk_save(&self, entities: &[LeadProvenanceRegistration]) -> Result<Vec<LeadProvenanceRegistration>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
