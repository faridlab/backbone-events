//! Repository trait for LeadRequest aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the LeadRequest aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::LeadRequest;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct LeadRequestPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl LeadRequestPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct LeadRequestPaginatedResult {
    pub data: Vec<LeadRequest>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct LeadRequestFilter {
    pub event_id: Option<Uuid>,
    pub done: Option<bool>,
    pub error_detail: Option<String>,
}

impl LeadRequestFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.event_id.is_some() || self.done.is_some() || self.error_detail.is_some()
    }
}

/// Repository trait for LeadRequest aggregate
///
/// This trait defines the persistence contract for LeadRequest entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait LeadRequestRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new lead_request entity
    async fn save(&self, entity: &LeadRequest) -> Result<LeadRequest>;

    /// Find lead_request by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<LeadRequest>>;

    /// Find all lead_request entities
    async fn find_all(&self) -> Result<Vec<LeadRequest>>;

    /// Update lead_request by ID
    async fn update(&self, id: &str, entity: &LeadRequest) -> Result<Option<LeadRequest>>;

    /// Delete lead_request by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List lead_request with pagination
    async fn list(&self, params: LeadRequestPaginationParams) -> Result<LeadRequestPaginatedResult>;

    /// List lead_request with pagination and filters
    async fn list_with_filters(&self, params: LeadRequestPaginationParams, filters: LeadRequestFilter) -> Result<LeadRequestPaginatedResult>;

    /// Count all lead_request entities
    async fn count(&self) -> Result<u64>;

    /// Count lead_request entities matching filters
    async fn count_with_filters(&self, filters: LeadRequestFilter) -> Result<u64>;

    /// Check if lead_request exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete lead_request by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted lead_request by ID
    async fn restore(&self, id: &str) -> Result<Option<LeadRequest>>;

    /// List soft-deleted lead_request entities
    async fn list_deleted(&self, params: LeadRequestPaginationParams) -> Result<LeadRequestPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find lead_request by event_id
    async fn find_by_event_id(&self, event_id: Uuid) -> Result<Option<LeadRequest>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if lead_request exists by event_id
    async fn exists_by_event_id(&self, event_id: Uuid) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save lead_request entities
    async fn bulk_save(&self, entities: &[LeadRequest]) -> Result<Vec<LeadRequest>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
