//! Repository trait for BoothCategory aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the BoothCategory aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::BoothCategory;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct BoothCategoryPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl BoothCategoryPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct BoothCategoryPaginatedResult {
    pub data: Vec<BoothCategory>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct BoothCategoryFilter {
    pub name: Option<String>,
    pub description: Option<String>,
    pub product_id: Option<Uuid>,
}

impl BoothCategoryFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.description.is_some() || self.product_id.is_some()
    }
}

/// Repository trait for BoothCategory aggregate
///
/// This trait defines the persistence contract for BoothCategory entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait BoothCategoryRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new booth_category entity
    async fn save(&self, entity: &BoothCategory) -> Result<BoothCategory>;

    /// Find booth_category by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<BoothCategory>>;

    /// Find all booth_category entities
    async fn find_all(&self) -> Result<Vec<BoothCategory>>;

    /// Update booth_category by ID
    async fn update(&self, id: &str, entity: &BoothCategory) -> Result<Option<BoothCategory>>;

    /// Delete booth_category by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List booth_category with pagination
    async fn list(&self, params: BoothCategoryPaginationParams) -> Result<BoothCategoryPaginatedResult>;

    /// List booth_category with pagination and filters
    async fn list_with_filters(&self, params: BoothCategoryPaginationParams, filters: BoothCategoryFilter) -> Result<BoothCategoryPaginatedResult>;

    /// Count all booth_category entities
    async fn count(&self) -> Result<u64>;

    /// Count booth_category entities matching filters
    async fn count_with_filters(&self, filters: BoothCategoryFilter) -> Result<u64>;

    /// Check if booth_category exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete booth_category by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted booth_category by ID
    async fn restore(&self, id: &str) -> Result<Option<BoothCategory>>;

    /// List soft-deleted booth_category entities
    async fn list_deleted(&self, params: BoothCategoryPaginationParams) -> Result<BoothCategoryPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save booth_category entities
    async fn bulk_save(&self, entities: &[BoothCategory]) -> Result<Vec<BoothCategory>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
