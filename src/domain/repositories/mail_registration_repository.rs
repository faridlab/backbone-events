//! Repository trait for MailRegistration aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the MailRegistration aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::MailRegistration;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailRegistrationPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl MailRegistrationPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct MailRegistrationPaginatedResult {
    pub data: Vec<MailRegistration>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MailRegistrationFilter {
    pub scheduler_id: Option<Uuid>,
    pub registration_id: Option<Uuid>,
    pub mail_sent: Option<bool>,
    pub outcome: Option<String>,
}

impl MailRegistrationFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.scheduler_id.is_some() || self.registration_id.is_some() || self.mail_sent.is_some() || self.outcome.is_some()
    }
}

/// Repository trait for MailRegistration aggregate
///
/// This trait defines the persistence contract for MailRegistration entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait MailRegistrationRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new mail_registration entity
    async fn save(&self, entity: &MailRegistration) -> Result<MailRegistration>;

    /// Find mail_registration by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<MailRegistration>>;

    /// Find all mail_registration entities
    async fn find_all(&self) -> Result<Vec<MailRegistration>>;

    /// Update mail_registration by ID
    async fn update(&self, id: &str, entity: &MailRegistration) -> Result<Option<MailRegistration>>;

    /// Delete mail_registration by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List mail_registration with pagination
    async fn list(&self, params: MailRegistrationPaginationParams) -> Result<MailRegistrationPaginatedResult>;

    /// List mail_registration with pagination and filters
    async fn list_with_filters(&self, params: MailRegistrationPaginationParams, filters: MailRegistrationFilter) -> Result<MailRegistrationPaginatedResult>;

    /// Count all mail_registration entities
    async fn count(&self) -> Result<u64>;

    /// Count mail_registration entities matching filters
    async fn count_with_filters(&self, filters: MailRegistrationFilter) -> Result<u64>;

    /// Check if mail_registration exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete mail_registration by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted mail_registration by ID
    async fn restore(&self, id: &str) -> Result<Option<MailRegistration>>;

    /// List soft-deleted mail_registration entities
    async fn list_deleted(&self, params: MailRegistrationPaginationParams) -> Result<MailRegistrationPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save mail_registration entities
    async fn bulk_save(&self, entities: &[MailRegistration]) -> Result<Vec<MailRegistration>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
