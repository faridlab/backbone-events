//! Repository for LeadRule entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<LeadRule, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::LeadRule;

/// Table name for LeadRule entities
pub const TABLE_NAME: &str = "event.lead_rules";

/// Repository for LeadRule entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct LeadRuleRepository(
    backbone_orm::GenericCrudRepository<LeadRule, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for LeadRuleRepository {
    type Target = backbone_orm::GenericCrudRepository<LeadRule, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl LeadRuleRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

backbone_core::impl_crud_repository!(LeadRuleRepository, LeadRule, soft_delete);
