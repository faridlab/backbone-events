//! Repository for Registration entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<Registration, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::Registration;

/// Table name for Registration entities
pub const TABLE_NAME: &str = "event.registrations";

/// Repository for Registration entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct RegistrationRepository(
    backbone_orm::GenericCrudRepository<Registration, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for RegistrationRepository {
    type Target = backbone_orm::GenericCrudRepository<Registration, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl RegistrationRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

impl RegistrationRepository {
    /// Find registration by barcode (excludes soft-deleted)
    pub async fn find_by_barcode(&self, barcode: &str) -> Result<Option<Registration>> {
        self.find_by_text_field("barcode", barcode).await
    }

    /// Check if registration exists by barcode (excludes soft-deleted)
    pub async fn exists_by_barcode(&self, barcode: &str) -> Result<bool> {
        self.exists_by_text_field("barcode", barcode).await
    }

}

backbone_core::impl_crud_repository!(RegistrationRepository, Registration, soft_delete);
