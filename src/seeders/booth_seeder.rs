//! Booth seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial Booth data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// BOOTH SEEDER
// ============================================================================

/// Seeder for booth
pub struct SeedBoothSeeder;

impl SeedBoothSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedBoothSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedBoothSeeder {
    fn name(&self) -> &'static str {
        "SeedBoothSeeder"
    }

    fn order(&self) -> i32 {
        2
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if event.booths table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM event.booths")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/booth_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM event.booths")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
