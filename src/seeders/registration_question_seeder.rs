//! RegistrationQuestion seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial RegistrationQuestion data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// REGISTRATIONQUESTION SEEDER
// ============================================================================

/// Seeder for registration_question
pub struct SeedRegistrationQuestionSeeder;

impl SeedRegistrationQuestionSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedRegistrationQuestionSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedRegistrationQuestionSeeder {
    fn name(&self) -> &'static str {
        "SeedRegistrationQuestionSeeder"
    }

    fn order(&self) -> i32 {
        9
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if event.registration_questions table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM event.registration_questions")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/registration_question_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM event.registration_questions")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
