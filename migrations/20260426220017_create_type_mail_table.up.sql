-- Migration: Create type_mails table
-- Generated by metaphor-schema

-- Create event_interval_unit enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'event_interval_unit') THEN
        CREATE TYPE event_interval_unit AS ENUM ('now', 'hours', 'days', 'weeks', 'months');
    END IF;
END
$$;

-- Create event_interval_kind enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'event_interval_kind') THEN
        CREATE TYPE event_interval_kind AS ENUM ('after_sub', 'before_event', 'after_event_start', 'after_event', 'before_event_end');
    END IF;
END
$$;

-- Create event_notification_channel enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'event_notification_channel') THEN
        CREATE TYPE event_notification_channel AS ENUM ('mail');
    END IF;
END
$$;

CREATE SCHEMA IF NOT EXISTS event;

CREATE TABLE IF NOT EXISTS event.type_mails (
    id UUID NOT NULL DEFAULT gen_random_uuid(),
    event_type_id UUID NOT NULL,
    interval_nbr INTEGER NOT NULL DEFAULT 1 CHECK (interval_nbr >= 0),
    interval_unit event_interval_unit NOT NULL DEFAULT 'now',
    interval_kind event_interval_kind NOT NULL DEFAULT 'after_sub',
    notification_channel event_notification_channel NOT NULL DEFAULT 'mail',
    template_ref UUID,
    template_kind TEXT,
    metadata JSONB NOT NULL DEFAULT '{"created_at":null,"updated_at":null,"deleted_at":null,"created_by":null,"updated_by":null,"deleted_by":null}'::jsonb,
    PRIMARY KEY (id)
);

CREATE INDEX IF NOT EXISTS idx_type_mails_event_type_id ON event.type_mails (event_type_id);

-- GIN index for audit metadata JSONB queries
CREATE INDEX IF NOT EXISTS idx_type_mails_metadata_gin ON event.type_mails USING GIN (metadata);
CREATE INDEX IF NOT EXISTS idx_type_mails_metadata_deleted_at ON event.type_mails ((metadata->>'deleted_at'));
CREATE INDEX IF NOT EXISTS idx_type_mails_metadata_created_at ON event.type_mails ((metadata->>'created_at'));
CREATE INDEX IF NOT EXISTS idx_type_mails_metadata_updated_at ON event.type_mails ((metadata->>'updated_at'));

-- Triggers for automatic metadata timestamp management
-- Automatically sets created_at on INSERT and updated_at on UPDATE

-- Function to set metadata->'created_at' on INSERT
CREATE OR REPLACE FUNCTION event.type_mails_audit_timestamp() RETURNS trigger AS $$
BEGIN
    IF TG_OP = 'INSERT' THEN
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{created_at}', to_jsonb(NOW()));
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{updated_at}', to_jsonb(NOW()));
    ELSIF TG_OP = 'UPDATE' THEN
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{updated_at}', to_jsonb(NOW()));
    END IF;
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

-- Trigger to set timestamps on INSERT
DROP TRIGGER IF EXISTS type_mails_insert_audit ON event.type_mails;
CREATE TRIGGER type_mails_insert_audit BEFORE INSERT ON event.type_mails
    FOR EACH ROW EXECUTE FUNCTION event.type_mails_audit_timestamp();

-- Trigger to set updated_at on UPDATE
DROP TRIGGER IF EXISTS type_mails_update_audit ON event.type_mails;
CREATE TRIGGER type_mails_update_audit BEFORE UPDATE ON event.type_mails
    FOR EACH ROW EXECUTE FUNCTION event.type_mails_audit_timestamp();

-- Inline foreign key constraints (forward + self refs)
ALTER TABLE event.type_mails ADD CONSTRAINT fk_type_mails_event_type_id FOREIGN KEY (event_type_id) REFERENCES event.types (id);
